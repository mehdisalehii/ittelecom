<?php

namespace App\Console;

use App\Console\Commands\RebuildAllCaches;
use App\Console\Commands\RebuildImageSizes;
use Illuminate\Console\Scheduling\Schedule;
use Illuminate\Foundation\Console\Kernel as ConsoleKernel;

class Kernel extends ConsoleKernel
{
    /**
     * The Artisan commands provided by your application.
     *
     * @var array
     */
    protected $commands = [
        RebuildImageSizes::class,
        RebuildAllCaches::class,
    ];

    /**
     * Define the application's command schedule.
     *
     * @param  \Illuminate\Console\Scheduling\Schedule  $schedule
     * @return void
     */
    protected function schedule(Schedule $schedule)
    {
        $schedule->command('ittelecom:rebuild-images')->hourly();
        $schedule->command('ittelecom:rebuild-caches')->hourlyAt(30);
    }

    /**
     * Register the Closure based commands for the application.
     *
     * @return void
     */
    protected function commands()
    {
        require base_path('routes/console.php');
    }
}
